import React, { ReactElement } from 'react';

import { StyledFormItem } from './style';

const FormGridItem = ({...props}): ReactElement => (
  <StyledFormItem>
  </StyledFormItem>
);

export default FormGridItem;
