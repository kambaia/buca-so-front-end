import styled from 'styled-components';

import theme from '../../../../styles/theme';

export const StyledFormGridTitle = styled.span`
  display: block;
  font-size: 1.2rem;
  font-weight: bold;
  margin-bottom: 1rem;
  color: ${theme.backgroundBlue};
`;
